<!DOCTYPE html>
  <html lang="en">
  <meta charset="UTF-8"/>
  <meta name = "viewport" content="width=device-width, initial-scale=1.0"/>
  <link rel  = "stylesheet" href = "AutoRepairStyle.css">
<head>
  <title>Main Page</title>
</head>
<body>
    <h1 id = h1>
    Auto Service and Repair
     <button id = login><a href="employeeDashboard"> Employee Login </a></button>
    </h1> 
         </div>


         <div id = mainInfo>

          <div id = MainServices>
              Services
          </div>
          
          <div id = mServiceList>
            
            <% data.forEach(item => { %>
              <%= JSON.stringify(item.task_name).replace(/"/g, '') %> 
              <p style="padding-left: 30px; font-size: 14px;"><%= JSON.stringify(item.task_description).replace(/"/g, '') %> </p>
            <% }); %>
              <br>
              <button id = scheduleButton><a href= "schedule">Schedule Visit</a></button>
              <br>
              <button id = custPay><a href= "customerLookup">Service History</a></button>
       </div>
          </div>
         </div>
    </body>
  </html>